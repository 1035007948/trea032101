<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.accounting.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.accounting.entity.User">
        <id column="id" property="id"/>
        <result column="account" property="account"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.accounting.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (account, password, nickname, create_time, update_time)
        VALUES (#{account}, #{password}, #{nickname}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.accounting.entity.User">
        UPDATE t_user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM t_user WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Long" resultMap="UserResultMap">
        SELECT * FROM t_user WHERE id = #{id}
    </select>

    <select id="selectByAccount" parameterType="java.lang.String" resultMap="UserResultMap">
        SELECT * FROM t_user WHERE account = #{account}
    </select>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT * FROM t_user
    </select>

    <update id="updatePassword">
        UPDATE t_user SET password = #{newPassword}, update_time = NOW() WHERE id = #{id}
    </update>

</mapper>
